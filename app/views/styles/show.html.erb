<h1><%= @style.name %></h1>

<p>
  <%= @style.description %>
</p>

<h3>Beers (<%= @style.beers.count %>):</h3>

<ul>
  <% @style.beers.each do |b| %>
    <li><%= link_to b.name, b %> by <%= link_to b.brewery.name, b.brewery %></li>
  <% end %>
</ul>

<%= link_to 'Back', styles_path %>
